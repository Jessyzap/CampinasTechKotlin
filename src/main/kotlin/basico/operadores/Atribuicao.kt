package basico.operadores

fun main() {

    var a : Int = 5
    var b : Int = 3

    // visual g cont <- cont + 1
    // visual g MOD  5 MOD 3 = 2

    // b += a // b = b + a
    // b -= a // b = b - a
    // b -= 4 // b = b - 4
    // b *= a // b = b * a
    // b /= a // b = b / a
    // a %= b // a = a % b MOD

    println(a)
}